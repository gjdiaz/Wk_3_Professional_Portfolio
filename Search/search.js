

var validChoices = ['projectName', 'projectNumber', 'startDate', 'finishDate', 'jobWalk', 'status'];

var projectName = "Project1";
var search = document.getElementById("search");

if (!validChoices.includes(user.choice))  {
    search.addEventListener.innerHTML ("click",function(){ 
    console.log("No results, please try again");
    })
};

